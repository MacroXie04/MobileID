# ==============================================
# MobileID Environment Configuration Template
# ==============================================
# This file contains the default values as read from your settings.py
# Copy this file to src/.env and update values as needed.

# ==============================================
# Core Settings
# ==============================================
# Django secret key - MUST be changed for production
SECRET_KEY=dev-secret

# Enable debug mode (True/False) - MUST be False in production
DEBUG=False

# Enable test mode (True/False) - Only set to True when running tests
TESTING=False

# Application timezone
TIME_ZONE=America/Los_Angeles

# ==============================================
# Network & CORS Settings
# ==============================================
# Comma-separated list of allowed hostnames
ALLOWED_HOSTS=localhost

# Backend API URL
BACKEND_ORIGIN=http://localhost:8000

# Comma-separated list of frontend URLs for CORS
# (Default from settings.py)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,http://127.0.0.1:8080

# Allow credentials in CORS requests (True/False)
CORS_ALLOW_CREDENTIALS=True

# Comma-separated list of trusted origins for CSRF
# (This is auto-generated from BACKEND_ORIGIN and CORS_ALLOWED_ORIGINS)
# CSRF_TRUSTED_ORIGINS=

# ==============================================
# Cookie Settings
# ==============================================
# SameSite attribute for session cookies (Lax/Strict/None)
COOKIE_SAMESITE=Lax

# SameSite attribute for CSRF cookies
CSRF_COOKIE_SAMESITE=Lax

# Require HTTPS for cookies (True/False) - Should be True in production
COOKIE_SECURE=False

# Make CSRF cookie HTTP-only (True/False)
CSRF_COOKIE_HTTPONLY=False

# Enable HTTPS-specific settings (True/False)
USE_HTTPS=False

# ==============================================
# Database Configuration
# ==============================================
# Choose one of two options:

# OPTION 1: Database URL (Recommended)
# ------------------------------------
# Database profile to use (local/gcp)
DB_PROFILE=local

# Database URL for local profile
# MySQL:      mysql://user:password@host:3306/dbname
# PostgreSQL: postgres://user:password@host:5432/dbname
# SQLite:     sqlite:///path/to/db.sqlite3
DATABASE_URL_LOCAL=

# Database URL for GCP Cloud SQL profile (Unix socket)
# DATABASE_URL_GCP=postgres://user:pass@/dbname?host=/cloudsql/PROJECT:REGION:INSTANCE

# OPTION 2: Discrete Variables (Fallback if URL is not set)
# --------------------------------------
# Database engine (postgresql/mysql/sqlite3)
# (Default from settings.py is postgresql)
# DB_ENGINE=postgresql

# Database name
# DB_NAME=

# Database user
# DB_USER=

# Database password
# DB_PASSWORD=

# Database host
# DB_HOST=

# Database port
# DB_PORT=

# Advanced Database Options
# --------------------------
# Persistent connection lifetime in seconds
DB_CONN_MAX_AGE=60

# SSL mode for database connections (empty/'require'/'verify-ca'/'verify-full')
DB_SSL_MODE=

# Disable SSL certificate verification (True/False)
DB_SSL_DISABLE_VERIFY=False

# Unix socket path for Cloud SQL connections
CLOUDSQL_UNIX_SOCKET=

# ==============================================
# Security Settings
# ==============================================
# Maximum failed login attempts before lockout
MAX_FAILED_LOGIN_ATTEMPTS=5

# Account lockout duration in minutes
ACCOUNT_LOCKOUT_DURATION=30

# ==============================================
# Cache & Session Settings
# ==============================================
# Django cache backend class
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache

# Cache location/identifier
CACHE_LOCATION=unique-snowflake

# Session backend
# (Default from settings.py when TESTING=False)
SESSION_ENGINE=django.contrib.sessions.backends.db

# ==============================================
# Production Checklist
# ==============================================
# Before deploying to production, ensure:
# 1. SECRET_KEY is changed to a strong random value
# 2. DEBUG is set to False
# 3. ALLOWED_HOSTS contains your domain name
# 4. CORS_ALLOWED_ORIGINS contains only your frontend domain
# 5. COOKIE_SECURE and USE_HTTPS are set to True
# 6. Database credentials are secure
# 7. SSL/TLS is configured (USE_HTTPS=True)
# 8. DB_SSL_MODE is set appropriately for production databases