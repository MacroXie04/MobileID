# -----------------------------------------------------------------------------
# D.J.A.N.G.O .E.N.V. T.E.M.P.L.A.T.E
# -----------------------------------------------------------------------------
# This file is an example. Copy it to a new file named .env in your project root
# and fill in the actual values for your development and production environments.
#
# IMPORTANT: DO NOT commit your .env file to version control.
# Add .env to your .gitignore file.
# -----------------------------------------------------------------------------

# --- Core Django Settings ---
# Generate a new secret key for each project.
# You can use an online generator or Django's `get_random_secret_key()` function.
SECRET_KEY=your_super_secret_key_goes_here

# Set to True for development and False for production.
# NEVER run with debug mode turned on in a production environment.
DEBUG=True

# A comma-separated list of hosts this Django site can serve.
# For development, you might use: localhost,127.0.0.1
# For production, set it to your domain names: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=127.0.0.1,localhost


# --- Database Configuration ---
# Default: SQLite (simpler, for local development)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# Example for PostgreSQL (uncomment to use)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432


# --- Static & Media Files (Example for AWS S3) ---
# Uncomment and configure if you are using a cloud storage provider.
# USE_S3=True
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_STORAGE_BUCKET_NAME=your_s3_bucket_name
# AWS_S3_REGION_NAME=us-east-1
# AWS_S3_CUSTOM_DOMAIN=%s.s3.amazonaws.com % AWS_STORAGE_BUCKET_NAME


# --- CORS (Cross-Origin Resource Sharing) ---
# If your frontend is on a different domain, list it here.
# Comma-separated list of origins.
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


# --- Third-Party Service API Keys ---
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key


# --- Celery & Redis (for background tasks) ---
# Uncomment if you are using Celery for asynchronous tasks.
# REDIS_URL=redis://localhost:6379/0
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

