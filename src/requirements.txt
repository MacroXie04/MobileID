Django>=5.2,<6.0
# REST framework
djangorestframework>=3.14,<4.0
# JWT
djangorestframework-simplejwt>=5.3,<6.0
# CORS
django-cors-headers>=4.3,<5.0
# Env loading
python-dotenv>=1.0
# Images
Pillow>=10.0,<11.0
# WebAuthn
webauthn>=1.11,<2.0
# Optional browser automation used by dynamic barcode code (safe even if disabled)
selenium>=4.0,<5.0
# Useful dev utilities referenced in settings
django-extensions>=3.2,<4.0
django-widget-tweaks>=1.5,<2.0
# WSGI server
gunicorn>=21.2,<22.0
# Redis cache backend (required for CI/CD and production)
redis>=5.0,<6.0

# Database URL parser and drivers
dj-database-url>=2.2
# Use binary mysqlclient to avoid compilation
mysqlclient>=2.2,<3.0
psycopg[binary]>=3.2

# ASGI worker for Gunicorn in prod override
uvicorn[standard]>=0.30,<1.0

# Selenium driver helper for ChromeDriver
webdriver-manager>=4.0,<5.0

