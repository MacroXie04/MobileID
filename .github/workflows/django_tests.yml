name: Django Tests

# Trigger after any backend lint workflow completes.
# The job will only run if the triggering lint workflow succeeded.
# Note: All lint workflows (Ruff, Black, Flake8) run in parallel,
# and tests will run after each completes successfully.
# Tests run on all branches for CI purposes.
on:
  workflow_run:
    workflows:
      - Lint Ruff
      - Lint Black
      - Lint Flake8
    types:
      - completed

jobs:
  test:
    # Only run if the triggering lint workflow succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        working-directory: ./src
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django tests with coverage
        working-directory: ./src
        env:
          TESTING: "True"
          DJANGO_SETTINGS_MODULE: "core.settings.dev"
        run: |
          python -m coverage run manage.py test -v 2
          python -m coverage xml -o coverage.xml

      - name: Upload coverage XML
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: src/coverage.xml
          retention-days: 30
